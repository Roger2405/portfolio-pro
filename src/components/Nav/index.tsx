import './styles.scss';

export default function Nav() {
    return (
        <nav className="navBar">
            <a href="#">sobre mim</a>
            <a href="#">formações</a>
            <a href="#">habilidades</a>
            <a href="#">projetos</a>
        </nav>
    )
}